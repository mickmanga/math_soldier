const topTextContainer = document.getElementById("top_map_container_a")!;
const worldImageContainer = document.getElementById("ice")!;

window.onload = () => {

  worldImageContainer.style.opacity = "1"; 
 topTextContainer.style.opacity = "1";
  
}